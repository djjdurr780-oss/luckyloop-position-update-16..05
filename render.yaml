services:
  - type: web
    name: luckyloop-job-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: jobs-db
      mountPath: /data
      sizeGB: 1
